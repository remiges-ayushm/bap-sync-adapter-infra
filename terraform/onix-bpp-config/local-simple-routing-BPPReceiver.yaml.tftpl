routingRules:
  - version: "2.0.0"
    targetType: "url"
    target:
      url: "${sandbox_bpp_url}/api/webhook" 
    endpoints:
      - select
      - init
      - confirm
      - status
      - cancel
      - support
